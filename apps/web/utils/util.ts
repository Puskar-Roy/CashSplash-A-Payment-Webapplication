import { Alata ,Ubuntu } from "next/font/google";

export const alata = Alata({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
export const ubuntu= Ubuntu({
  weight: "700",
  subsets: ["latin"],
  display: "swap",
});
